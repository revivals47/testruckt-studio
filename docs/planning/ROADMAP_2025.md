# Testruct Studio Rust版 - 今後のロードマップ 2025年11月-2026年

**作成日**: 2025年11月8日
**現在の進捗**: 50% (15/30機能実装完了)
**テーム規模**: 1人 (Claude Code)
**推定完了**: 2025年12月末

---

## 📊 全体進捗サマリー

| 区分 | 完成度 | 状態 |
|------|--------|------|
| **基本機能** (Canvas, Input, Text) | ✅ 100% | 完了 |
| **中級機能** (Selection, Tools, Properties) | ✅ 100% | 完了 |
| **スタイル機能** (Fill, Stroke, Typography) | ⏳ 0% | 次フェーズ |
| **高度な機能** (Layers, Grouping, Templates) | ⏳ 20% | 進行中 |
| **エクスポート** (PDF, Image, SVG) | ✅ 100% | 完了 |
| **ドキュメント管理** (Save/Load) | ✅ 50% | 進行中 |

**総実装行数**: 11,451行 / 推定32,050行 (35%)

---

## 🎯 Phase 1: スタイル機能の完成 (2週間)

**目標完了日**: 2025年11月22日
**優先度**: 🔴 高

### 1.1 Fill & Stroke プロパティの完全実装

**現状**:
- ✅ UI は完成（カラーピッカー連携）
- ✅ データ構造は定義済み
- ⏳ ドキュメント永続化が必要
- ⏳ ビジュアルフィードバックが不完全

**実装内容**:

```rust
// crates/core/src/document.rs に追加
pub struct ShapeStyle {
    pub fill: FillStyle,           // Solid, Gradient, None
    pub stroke: StrokeStyle,       // Color, Width, DashArray
    pub stroke_width: f32,
    pub stroke_color: Color,
    pub opacity: f32,              // 0.0 - 1.0
}

pub enum FillStyle {
    Solid(Color),
    Gradient(GradientFill),
    None,
}

pub struct StrokeStyle {
    pub color: Color,
    pub width: f32,
    pub dash_array: Option<Vec<f32>>,  // [5, 5] for dashed
    pub line_cap: LineCap,
    pub line_join: LineJoin,
}
```

**実装ステップ**:
1. `ShapeStyle` の永続化（Save/Load 対応）
2. プロパティパネルの値 → ドキュメント同期
3. Canvas 描画時に `ShapeStyle` を適用
4. リアルタイムプレビュー

**ファイル**:
- `crates/core/src/document.rs`: データ構造
- `crates/ui/src/canvas/shapes_rendering.rs`: 描画ロジック
- `crates/ui/src/panels/properties.rs`: UI 同期

**チェックリスト**:
- [ ] ShapeStyle データ構造実装
- [ ] カラーピッカーから値取得
- [ ] ドキュメント永続化
- [ ] Canvas 描画への適用
- [ ] リアルタイムプレビュー実装
- [ ] ユニットテスト作成

**見積り**: 2-3日

---

### 1.2 Typography 完全対応

**現状**:
- ✅ フォント選択機能
- ✅ フォントサイズ変更
- ⏳ 太字・斜体・下線が不完全
- ⏳ 行間隔が基本的

**実装内容**:

```rust
// crates/core/src/typography.rs 拡張
pub struct TextStyle {
    pub font_family: String,
    pub font_size: f32,
    pub font_weight: FontWeight,      // Normal, Bold, etc
    pub font_style: FontStyle,        // Normal, Italic, Oblique
    pub text_decoration: TextDecoration,  // None, Underline, LineThrough
    pub color: Color,
    pub alignment: TextAlignment,     // Start, Center, End, Justified
    pub line_height: f32,
    pub letter_spacing: f32,
}

pub enum FontWeight {
    Thin,       // 100
    ExtraLight, // 200
    Light,      // 300
    Normal,     // 400
    Medium,     // 500
    SemiBold,   // 600
    Bold,       // 700
    ExtraBold,  // 800
    Black,      // 900
}
```

**実装ステップ**:
1. フォント属性の Pango 統合
2. プロパティパネルの UI 追加（ボタン）
3. テキスト要素への適用
4. リアルタイムプレビュー

**ファイル**:
- `crates/core/src/typography.rs`: データ構造
- `crates/ui/src/canvas/shapes_rendering.rs`: Pango 統合
- `crates/ui/src/panels/properties.rs`: UI ボタン

**チェックリスト**:
- [ ] FontWeight/FontStyle enum 実装
- [ ] Pango 属性マッピング
- [ ] プロパティパネルボタン追加
- [ ] テキスト描画への適用
- [ ] リアルタイムプレビュー
- [ ] ユニットテスト

**見積り**: 2-3日

---

## 🎨 Phase 2: 高度な機能 (3週間)

**目標完了日**: 2025年12月13日
**優先度**: 🟡 中

### 2.1 Layers パネルの完全実装

**現状**:
- ✅ UI レイアウト（層表示）
- ⏳ ドラッグ&ドロップが未実装
- ⏳ 層の並び替えが未実装
- ⏳ 層の表示/非表示が未実装

**実装内容**:

```rust
// crates/ui/src/panels/layers.rs 拡張
pub struct LayerPanel {
    list_box: gtk4::ListBox,
    items: RefCell<Vec<LayerItem>>,
    selected_layer: RefCell<Option<Uuid>>,
}

impl LayerPanel {
    pub fn refresh_from_document(&self, doc: &Document) {
        // ドキュメント → パネル同期
    }

    pub fn on_layer_visibility_toggled(&self, layer_id: Uuid, visible: bool) {
        // 表示/非表示の反映
    }

    pub fn on_layer_reordered(&self, from: usize, to: usize) {
        // レイヤー順序変更を apply_command で適用
    }

    pub fn on_layer_selected(&self, layer_id: Uuid) {
        // キャンバスの選択を同期
    }
}
```

**実装ステップ**:
1. `Page.elements` の順序を UI に反映
2. Drag & Drop でレイヤー順序変更
3. 表示/非表示トグルの実装
4. レイヤー名の編集機能
5. キャンバス選択との双方向同期

**ファイル**:
- `crates/ui/src/panels/layers.rs`: パネル実装
- `crates/ui/src/canvas/mod.rs`: ドキュメント同期

**チェックリスト**:
- [ ] Drag & Drop 実装
- [ ] 表示/非表示トグル
- [ ] レイヤー名編集
- [ ] レイヤー順序の永続化
- [ ] キャンバス選択同期
- [ ] ユニットテスト

**見積り**: 3-4日

---

### 2.2 Grouping 機能

**現状**:
- ❌ 未実装

**実装内容**:

```rust
// crates/core/src/document.rs に追加
pub struct Group {
    pub id: Uuid,
    pub name: String,
    pub children: Vec<Uuid>,  // 子要素の ID
    pub bounds: Rect,
    pub visible: bool,
    pub locked: bool,
}

pub enum DocumentElement {
    Shape(ShapeElement),
    Text(TextElement),
    Image(ImageElement),
    Frame(Frame),
    Group(Group),  // ← 新規追加
}
```

**実装ステップ**:
1. Group データ構造
2. Grouping コマンド (create_group, ungroup)
3. グループ内の要素の hit test
4. キャンバス描画時のグループ処理
5. プロパティパネル表示

**ファイル**:
- `crates/core/src/document.rs`: Group 構造
- `crates/ui/src/canvas/selection.rs`: グループ選択処理
- `crates/ui/src/canvas/tools.rs`: grouping コマンド

**チェックリスト**:
- [ ] Group データ構造実装
- [ ] Create/Ungroup コマンド
- [ ] Hit test 対応
- [ ] グループ内移動
- [ ] プロパティパネル
- [ ] ユニットテスト

**見積り**: 4-5日

---

### 2.3 Template システムの拡張

**現状**:
- ✅ テンプレート保存基本機能
- ⏳ テンプレートライブラリUI が不完全
- ⏳ テンプレートプレビューがない
- ⏳ カテゴリ分類がない

**実装内容**:

```rust
// crates/ui/src/dialogs/template_dialog.rs
pub struct TemplateDialog {
    category_filter: gtk4::ComboBoxText,
    template_list: gtk4::ListBox,
    preview_canvas: gtk4::DrawingArea,
    description: gtk4::Label,
}

impl TemplateDialog {
    pub fn load_templates(&self) -> Result<()> {
        // テンプレートファイル読み込み
    }

    pub fn show_preview(&self, template: &Template) {
        // プレビューキャンバス描画
    }

    pub fn apply_template(&self, template: Template) -> Result<()> {
        // テンプレート適用
    }
}
```

**実装ステップ**:
1. テンプレートメタデータ（名前、カテゴリ、説明）
2. テンプレート一覧表示UI
3. テンプレートプレビュー表示
4. カテゴリでフィルタリング
5. テンプレート適用時の処理

**ファイル**:
- `crates/ui/src/dialogs/template_dialog.rs`: UI
- `crates/ui/src/templates.rs`: テンプレート管理

**チェックリスト**:
- [ ] テンプレートメタデータ
- [ ] 一覧UI実装
- [ ] プレビュー表示
- [ ] カテゴリフィルタ
- [ ] 適用処理
- [ ] ユニットテスト

**見積り**: 2-3日

---

## 📦 Phase 3: Save/Load の完全実装 (1週間)

**目標完了日**: 2025年12月20日
**優先度**: 🔴 高

### 3.1 JSON Serialization の完全化

**現状**:
- ✅ 基本構造のシリアライズ
- ⏳ 全ての要素タイプが対応していない
- ⏳ エラーハンドリングが不完全
- ⏳ バージョン管理がない

**実装内容**:

```rust
// crates/ui/src/io/save.rs
pub struct DocumentSaver {
    document: Document,
    asset_catalog: AssetCatalog,
}

impl DocumentSaver {
    pub fn save_to_file(&self, path: &Path) -> Result<()> {
        // JSON化 + 画像アセット保存
    }

    pub fn save_to_json(&self) -> Result<String> {
        // ドキュメント → JSON
    }
}

// crates/ui/src/io/load.rs
pub struct DocumentLoader;

impl DocumentLoader {
    pub fn load_from_file(path: &Path) -> Result<Document> {
        // ファイル読み込み + アセット復元
    }

    pub fn load_from_json(json: &str) -> Result<Document> {
        // JSON → ドキュメント
    }
}
```

**実装ステップ**:
1. 全要素タイプの Serialize/Deserialize 実装
2. Asset アセット参照の保存
3. エラーハンドリング強化
4. バージョン情報の保存
5. 後方互換性対応

**ファイル**:
- `crates/ui/src/io/save.rs`: 保存処理
- `crates/ui/src/io/load.rs`: 読み込み処理

**チェックリスト**:
- [ ] 全要素の Serialize 実装
- [ ] Asset 参照の保存
- [ ] エラーハンドリング
- [ ] バージョン管理
- [ ] 後方互換性テスト
- [ ] ユニットテスト

**見積り**: 2-3日

---

### 3.2 Auto-Save & Recovery

**現状**:
- ❌ 未実装

**実装内容**:

```rust
// crates/ui/src/autosave.rs
pub struct AutoSaveManager {
    save_interval: Duration,
    recovery_dir: PathBuf,
    last_save: RefCell<Instant>,
}

impl AutoSaveManager {
    pub fn start(&self, app_state: AppState) {
        // タイマー開始
    }

    pub fn save_recovery_point(&self, doc: &Document) -> Result<()> {
        // リカバリーポイント保存
    }

    pub fn restore_from_recovery() -> Option<Document> {
        // リカバリーファイルから復元
    }
}
```

**実装ステップ**:
1. 定期的なオートセーブ実装（5分ごと）
2. リカバリーファイルディレクトリ
3. アプリ起動時のリカバリーチェック
4. リカバリーダイアログUI

**ファイル**:
- `crates/ui/src/autosave.rs`: オートセーブ管理

**チェックリスト**:
- [ ] タイマー実装
- [ ] リカバリーファイル保存
- [ ] リカバリー復元ロジック
- [ ] UI ダイアログ
- [ ] テスト

**見積り**: 1-2日

---

## 🚀 Phase 4: パフォーマンス最適化 (2週間)

**目標完了日**: 2025年12月末
**優先度**: 🟡 中

### 4.1 描画パフォーマンスの最適化

**現状**:
- ✅ 基本的な描画フロー
- ⏳ 大量オブジェクト時に遅い可能性
- ⏳ 部分更新（dirty region）が未実装
- ⏳ キャッシング戦略がない

**実装内容**:

```rust
// crates/ui/src/canvas/dirty_region.rs 拡張
pub struct DirtyRegionManager {
    regions: RefCell<Vec<Rect>>,
    threshold: usize,  // リージョン数の閾値
}

impl DirtyRegionManager {
    pub fn mark_dirty(&self, rect: Rect) {
        // 更新領域を記録
    }

    pub fn should_full_redraw(&self) -> bool {
        // リージョン数が多すぎれば全体描画
    }

    pub fn get_dirty_regions(&self) -> Vec<Rect> {
        // 描画対象リージョン取得
    }

    pub fn clear(&self) {
        // クリア
    }
}
```

**実装ステップ**:
1. 更新領域の追跡
2. 描画時に該当領域のみ更新
3. キャッシュの活用
4. レンダリングパイプラインの最適化
5. メモリ使用量の最適化

**ファイル**:
- `crates/ui/src/canvas/dirty_region.rs`: 既に基本実装あり
- `crates/ui/src/canvas/rendering.rs`: 描画ロジック

**チェックリスト**:
- [ ] Dirty region トラッキング
- [ ] 部分描画実装
- [ ] キャッシング戦略
- [ ] パフォーマンステスト
- [ ] メモリプロファイリング

**見積り**: 3-4日

---

### 4.2 ズームレベルの最適化

**現状**:
- ✅ ズーム機能基本実装
- ⏳ 大きなズームでの描画品質が低い可能性
- ⏳ ズームレベルの保存がない

**実装内容**:

```rust
// crates/ui/src/canvas/rendering.rs 内のズーム処理
pub struct ZoomManager {
    current_zoom: f32,
    min_zoom: f32,
    max_zoom: f32,
}

impl ZoomManager {
    pub fn set_zoom(&mut self, zoom: f32) {
        // ズームレベル設定（0.1 - 5.0）
    }

    pub fn zoom_to_fit(&mut self, bounds: Rect, canvas_size: (f32, f32)) {
        // コンテンツに合わせてズーム
    }

    pub fn zoom_to_selection(&mut self, selected_bounds: Rect) {
        // 選択要素に合わせてズーム
    }
}
```

**実装ステップ**:
1. ズーム値の保存・復元
2. Zoom to Fit 機能
3. Zoom to Selection 機能
4. 段階的ズーム（25%, 50%, 75%, 100%, 150%, 200%）
5. キーボードショートカット

**ファイル**:
- `crates/ui/src/canvas/rendering.rs`: ズームロジック

**チェックリスト**:
- [ ] ズーム値の保存
- [ ] Zoom to Fit
- [ ] Zoom to Selection
- [ ] キーボードショートカット
- [ ] パフォーマンステスト

**見積り**: 2-3日

---

## 📋 Phase 5: テスト & ドキュメント (2週間)

**目標完了日**: 2026年1月15日
**優先度**: 🟡 中

### 5.1 単体テスト（Unit Tests）の拡充

**現状**:
- ✅ 52個のテスト合格
- ⏳ カバレッジが不完全（特にCanvas機能）
- ⏳ 統合テストが少ない

**実装内容**:

```rust
// テスト対象モジュール一覧
#[cfg(test)]
mod tests {
    // canvas/input/keyboard.rs テスト
    // canvas/input/gesture.rs テスト
    // canvas/rendering.rs テスト
    // document.rs テスト
    // properties.rs テスト
    // alignment.rs テスト
    // snapping.rs テスト
    // grouping.rs テスト
}
```

**実装ステップ**:
1. 各モジュールの単体テスト追加
2. 統合テストの作成
3. エッジケースのテスト
4. パフォーマンステスト
5. テストカバレッジ測定

**目標**: テストカバレッジ 80% 以上

**見積り**: 3-4日

---

### 5.2 ユーザードキュメント整備

**現状**:
- ✅ 開発者向けドキュメント
- ⏳ ユーザー向けマニュアルが不十分

**実装内容**:

1. **クイックスタートガイド**
   - インストール方法
   - 基本操作

2. **機能別マニュアル**
   - 図形作成
   - テキスト編集
   - スタイル変更
   - レイヤー管理
   - テンプレート使用

3. **FAQ**
   - よくある質問
   - トラブルシューティング

4. **API ドキュメント**
   - Rust doc 生成（cargo doc）

**見積り**: 2-3日

---

## 🎓 Phase 6: 拡張機能 (オプション、2026年)

**優先度**: 🟢 低
**実装時期**: 2026年1月以降（必要に応じて）

### 6.1 マルチページ対応

```rust
// 複数ページ管理
pub fn create_new_page(document: &mut Document) -> Uuid
pub fn delete_page(document: &mut Document, page_id: Uuid) -> Result<()>
pub fn reorder_pages(document: &mut Document, from: usize, to: usize)
pub fn rename_page(document: &mut Document, page_id: Uuid, name: String)
```

**見積り**: 3-4日

---

### 6.2 図形プリセット & シンボル

```rust
// シンボルライブラリ
pub struct SymbolLibrary {
    symbols: HashMap<String, Symbol>,
}

pub fn insert_symbol(document: &mut Document, symbol_id: &str) -> Result<()>
pub fn create_custom_symbol(name: String, element: DocumentElement) -> Symbol
```

**見積り**: 2-3日

---

### 6.3 共同編集基盤（将来）

- CRDT（Conflict-free Replicated Data Type）の検討
- ネットワーク同期機構
- ユーザー権限管理

**見積り**: 1-2週間（要検討）

---

## 📈 期間別マイルストーン

| マイルストーン | 完了日 | 機能 |
|---------------|--------|------|
| Phase 1 完了 | 2025-11-22 | Fill/Stroke, Typography |
| Phase 2-3 完了 | 2025-12-20 | Layers, Grouping, Save/Load |
| Phase 4 完了 | 2025-12-31 | パフォーマンス最適化 |
| Phase 5 完了 | 2026-01-15 | テスト、ドキュメント |
| **v1.0 リリース** | **2026-01-31** | 全機能完成 |

---

## 🔧 技術的負債の解決

### 優先度: 高

1. **入力処理モジュール** ✅ 完了 (2025-11-08)
   - キーボード、マウス、ジェスチャーの分離
   - ドキュメント整備完了
   - keyboard.rs を 3つの焦点化されたモジュールに分割
     - keyboard/mod.rs (222行): イベント振り分け
     - keyboard/text_editing_keys.rs (255行): テキスト編集キー処理
     - keyboard/text_alignment_keys.rs (77行): テキスト配置キー処理

2. **テキスト編集機能の問題調査** ⏳ 進行中
   - **既知の問題**: 初回作成テキストボックスでキー入力が反応しないことがある
   - **状態**: テキスト編集モード入場は正常、キーボード入力の取得に問題がある可能性
   - **次ステップ**:
     1. GTK4 キーボードイベントハンドラーの調査
     2. drawing_area フォーカス取得の確認
     3. EventControllerKey のイベント伝播確認
     4. 複数テキストボックス時の focus 管理改善
   - **見積り**: 1-2日

3. **日本語IME入力対応** ⏳ 計画中
   - **現状**: `keyval.to_unicode()` では IME確定入力が取得できない
   - **実装予定**: GTK4 IMContext の統合
   - **見積り**: 2-3日
   - **優先度**: 中（オプション機能）

4. **エラーハンドリング強化**
   - Result/Error 型の統一
   - エラーメッセージの改善

5. **型安全性の向上**
   - Clippy 警告の解決
   - メモリ安全性の確保

### 優先度: 中

6. **テストカバレッジ**
   - 現在: 52テスト
   - 目標: 200+ テスト（80% カバレッジ）

7. **ドキュメント**
   - 開発者向け: ✅ 完了
   - ユーザー向け: ⏳ 進行中

---

## 📊 リソース管理

### チーム規模
- **開発**: 1人 (Claude Code)
- **テスト**: 自動テスト + 手動確認
- **ドキュメント**: 開発と並行

### 工数（人日）

| フェーズ | 見積り | 実績 |
|---------|--------|------|
| Phase 1 | 5日 | TBD |
| Phase 2 | 10日 | TBD |
| Phase 3 | 5日 | TBD |
| Phase 4 | 7日 | TBD |
| Phase 5 | 7日 | TBD |
| **合計** | **34日** | **TBD** |

---

## 🎯 成功基準

### 機能面
- [ ] 30/30 機能実装完了
- [ ] テスト合格率 100%
- [ ] カバレッジ 80% 以上

### 品質面
- [ ] Clippy 警告 0件
- [ ] 未処理の panic なし
- [ ] メモリリーク 0件

### ユーザー体験
- [ ] レスポンス時間 < 100ms
- [ ] 大量オブジェクト（1000+）でも動作
- [ ] ドキュメント完全（API + ユーザー向け）

---

## 💡 意思決定ポイント

### 1. Undo/Redo の詳細度
**選択肢**:
- A. 細粒度（各キー入力ごと）→ 実装が複雑
- B. 粗粒度（各操作ごと）→ シンプル、現在の実装

**推奨**: B（粗粒度）で開始、後に調整可能

### 2. Save フォーマット
**選択肢**:
- A. JSON（人間が読める）✅ 採用
- B. バイナリ（高速）
- C. 両対応

**推奨**: A（JSON）で統一

### 3. マルチページ対応の時期
**選択肢**:
- A. 今すぐ（基本設計に組込）
- B. v1.0 後（Phase 6）✅ 推奨
- C. 検討中止

**推奨**: B（v1.0 後）

---

## 📞 フィードバックと調整

このロードマップは以下の基準で定期的に見直します：

- **週1回**: マイルストーン達成度の確認
- **月1回**: 全体進捗と優先度の再評価
- **必要に応じて**: 技術的課題への対応

---

## 関連ドキュメント

- [FEATURE_LIST.md](./FEATURE_LIST.md) - 機能一覧と実装状況
- [FEATURE_COMPLETENESS_COMPARISON.md](./FEATURE_COMPLETENESS_COMPARISON.md) - オリジナルとの比較
- [REFACTORING_ROADMAP.md](./REFACTORING_ROADMAP.md) - リファクタリング計画
- [crates/ui/src/canvas/ARCHITECTURE.md](./crates/ui/src/canvas/ARCHITECTURE.md) - キャンバスアーキテクチャ
- [crates/ui/src/canvas/INPUT_MODULE.md](./crates/ui/src/canvas/INPUT_MODULE.md) - 入力モジュール詳細

---

**最終更新**: 2025年11月8日
**作成者**: Claude Code
**Status**: 公開版

